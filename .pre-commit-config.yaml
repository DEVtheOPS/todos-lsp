repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-merge-conflict
      - id: check-case-conflict
      - id: check-json
        # SvelteKit-generated tsconfig files are JSONC (allow comments).
        exclude: ^website/tsconfig\.json$
      - id: check-yaml
      - id: end-of-file-fixer
        exclude: ^assets/.*\.import$
      - id: trailing-whitespace
        args: [--markdown-linebreak-ext=md]
        exclude: ^assets/.*\.import$
      - id: mixed-line-ending
        args: [--fix=lf]
      - id: detect-private-key

  - repo: https://github.com/igorshubovych/markdownlint-cli
    rev: v0.48.0
    hooks:
      - id: markdownlint
        args: [--fix]
        files: ^(docs/.*\.md|AGENTS\.md)$

  - repo: https://github.com/pre-commit/mirrors-prettier
    rev: v4.0.0-alpha.8
    hooks:
      - id: prettier

  # - repo: local
  #   hooks:
  #     - id: obsidian-home-bookmark
  #       name: Ensure Obsidian bookmark points to Home.md
  #       entry: >-
  #         bash -c "grep -q '\"path\": \"Home.md\"'
  #         docs/.obsidian/bookmarks.json"
  #       language: system
  #       pass_filenames: false
